# Optional. If unset, the app calls http://<same-host-as-the-page>:8080/api/v1
# so localhost and LAN (phone) both work. Set only to force a fixed API URL.
# VITE_API_URL=http://localhost:8080/api/v1
