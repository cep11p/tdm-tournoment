version: '3.9'

services:

  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      args:
        TZ: ${APP_TIMEZONE:-America/Argentina/Buenos_Aires}
    container_name: tdm-api
    user: "${UID}:${GID}"
    working_dir: /var/www/html
    environment:
      TZ: ${APP_TIMEZONE:-America/Argentina/Buenos_Aires}
    volumes:
      - ./backend:/var/www/html
      - ./docker/php/conf.d/99-timezone.ini:/usr/local/etc/php/conf.d/99-timezone.ini:ro
    depends_on:
      - db
    networks:
      - tdm-network

  nginx:
    image: nginx:alpine
    container_name: tdm-nginx
    ports:
      - "8080:80"
    volumes:
      - ./backend:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - tdm-network

  db:
    image: mariadb:11
    container_name: tdm-db
    restart: unless-stopped
    environment:
      TZ: ${APP_TIMEZONE:-America/Argentina/Buenos_Aires}
      MYSQL_DATABASE: tdm
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: tdm
      MYSQL_PASSWORD: secret
    ports:
      - "3307:3306"
    volumes:
      - dbdata:/var/lib/mysql
      - ./docker/mariadb/conf.d:/etc/mysql/mariadb.conf.d:ro
    networks:
      - tdm-network

  redis:
    image: redis:alpine
    container_name: tdm-redis
    ports:
      - "6379:6379"
    networks:
      - tdm-network

networks:
  tdm-network:

volumes:
  dbdata:
